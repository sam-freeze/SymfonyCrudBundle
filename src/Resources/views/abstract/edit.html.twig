{% extends 'hero.html.twig' %}

{% block title %}{% if item.id %} {{ "#{name}.edit"|trans({}, 'SymfonyCrudBundle') }} {% else %} {{ "#{name}.create"|trans({}, 'SymfonyCrudBundle') }} {% endif %}{% endblock %}

{% block body %}
    <div class="column is-6 is-offset-3 has-text-centered">
        <h2 class="title">{% if item.id %} {{ "#{name}.edit"|trans({}, 'SymfonyCrudBundle') }} {% else %} {{ "#{name}.create"|trans({}, 'SymfonyCrudBundle') }} {% endif %}</h2>

        <div class="box">
            {% include "#{name}/_form.html.twig" %}
        </div>

        <p>
            <a href="{{ path("#{route}index") }}">
                {{ "#{name}.back"|trans({}, 'SymfonyCrudBundle') }}
            </a>
        </p>
    </div>
{% endblock %}
